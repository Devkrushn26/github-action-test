name : Shell Commmands

on : [push]

jobs: 
     run-shell-command:
         runs-on : ubuntu-latest
         steps:
             - name : echo a string
               run : echo "I'm groot"
             - name : multine scipt
               run : |
                     node -v
                     npm -v 